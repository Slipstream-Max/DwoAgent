Channel tools are only available on turns that are triggered by an external
channel such as Weixin or Feishu. When the current message includes a
`<channel_context>` block and a matching channel reply tool is available, use
that tool to send requested local files, images, or cards back to the current
channel conversation.

Do not assume channel reply tools are available unless they are present in the
current tool list.
