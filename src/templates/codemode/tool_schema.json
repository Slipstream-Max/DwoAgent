[
  {
    "type": "function",
    "function": {
      "name": "exec_chain",
      "description": "Execute short-lived Monty code in a sandbox. MCP capabilities are available only inside the code through injected synchronous functions.",
      "parameters": {
        "type": "object",
        "properties": {
          "code": {
            "type": "string",
            "description": "Monty Python code to execute. Use print() for output."
          },
          "timeout": {
            "type": "number",
            "description": "Total execution timeout in seconds.",
            "default": 30,
            "minimum": 0.1
          },
          "outputlimit": {
            "type": "integer",
            "description": "Maximum number of output characters returned.",
            "default": 12000,
            "minimum": 1
          }
        },
        "required": [
          "code"
        ],
        "additionalProperties": false
      }
    }
  }
]
