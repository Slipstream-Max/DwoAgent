enabled: false
jobs:
  - id: daily_digest
    enabled: true
    workspace_dir: .
    session:
      mode: new
    schedule:
      type: interval
      every_seconds: 3600
    prompt: "总结当前项目状态。"
    notify:
      - channel: weixin
