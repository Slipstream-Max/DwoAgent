[
  {
    "type": "function",
    "function": {
      "name": "weixin_reply_media",
      "description": "Reply to the current Weixin conversation with a local media file. The path must be inside the configured workspace or the active session directory.",
      "parameters": {
        "type": "object",
        "additionalProperties": false,
        "properties": {
          "path": {
            "type": "string",
            "description": "Workspace-relative or absolute path to the file to send."
          }
        },
        "required": [
          "path"
        ]
      }
    }
  }
]
